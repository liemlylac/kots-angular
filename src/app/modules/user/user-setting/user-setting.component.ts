import { Component, OnInit } from '@angular/core';

@Component({
  selector: 'app-user-setting',
  template: `user setting page is loaded`
})
export class UserSettingComponent implements OnInit {
  constructor(
  ) {
  }

  ngOnInit(): void {
  }
}
